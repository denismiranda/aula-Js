 // Manipulando Arrays

 // Criar array com constructor

/*  
    let myArray = new Array('a', 'b', 'c')
    console.log(myArray)
*/

 // Contar elementos de um array

/*
    console.log(["a","b", "c") //  console.log(["a",{type: "array"},function(){return "alo"}, "c"])
*/

 // Transformar uma cadeia de caracteres em elementos de um array

/*
    let word = "manipulation"
    console.log(Array.from(word))
*/

 // ##

    // let techs = ["HTML", "CSS", "JS"]
    // techs.push("NodeJs") // add item no final do array

    // techs.unshift("SQL") // add item no início do array

    // techs.pop() // remove item no final do array

    // techs.shift() // removem item no início do array

    // console.log(techs.slice(1,3)) // pegar somente alguns elementos do array

    // techs.splice(1, 1) // remove 1 ou mais itens em qualquer posição do array

/*    
    let index = techs.indexOf("SQL") // encontra a posição de um elemento no array
    techs.splice(index, 0)
    console.log(index)
*/